name: Check spelling

on:
  pull_request:

jobs:
  check_spelling:
    runs-on: ubuntu-latest
    steps:
    - name: Set up Git repository
      uses: actions/checkout@v6
    - name: Check spelling
      uses: crate-ci/typos@v1.47.2
